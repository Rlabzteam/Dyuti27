/*
 * Accommodation page
 *
 * The original React component does not require React state,
 * effects, or client-side rendering for this page.
 *
 * The PDF and email actions are regular HTML links, so no
 * JavaScript is required for their functionality.
 */

document.addEventListener("DOMContentLoaded", function () {
  // Accommodation page initialization.
});